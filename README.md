# Virtual-Banking-System-Sign-Up-page
A working sign-up web page for a Virtual Banking System. The frontend is built using HTML, while the backend is developed in Java to handle form processing and validation. User registration data is stored using both SQL and MongoDB, demonstrating full-stack development and multi-database integration.
